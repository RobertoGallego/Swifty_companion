import React from 'react'

import {
    SafeAreaView,
    ScrollView,
    Text,
    View,
} from 'react-native';

const Searchpage: React.FC = ({}) => {
    return (
        <SafeAreaView>
            <ScrollView>
                <View>
                    <Text>SEARCH</Text>
                </View>
            </ScrollView>
        </SafeAreaView>
    )
}

export default Searchpage
